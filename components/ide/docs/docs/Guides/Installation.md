# Installation

Copy this folder to your PHP web server, for example:

```bash
/var/www/html/docs-app
```

Make sure PHP can read the files inside the `docs` folder.

## Requirements

- PHP 8 or newer recommended
- Apache or Nginx
- Parsedown included in `vendor/parsedown/Parsedown.php`

No database is required.
